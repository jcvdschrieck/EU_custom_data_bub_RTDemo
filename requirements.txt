# ── EU Custom Data Hub API ────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
aiofiles>=23.0

# ── VAT Fraud Detection agent (submodule: vat_fraud_detection/) ───────────────
anthropic>=0.28
chromadb>=0.5
sentence-transformers>=3.0
python-dotenv>=1.0
openai>=1.0          # LM Studio OpenAI-compatible API (optional local model)
pdfplumber>=0.11
Pillow>=10.0
requests>=2.32
tenacity>=8.3
lxml>=5.2

# ── Legacy Streamlit pages (optional) ────────────────────────────────────────
streamlit>=1.33.0
plotly>=5.20.0
pandas>=2.2.0
openpyxl>=3.1
